package day42;

import day39.Student;

public class ConstructorIntro {

    // everytime we create an object a special block of code is called
    // it is NOT a method!
    // it does not have RETURN type
    //It is the same name as Class

    // if there is NEW keyword == java is seraching for constructor to create that object

    Student s1 = new Student() ;

    // this above calls constructor automatically : then it is DEFAULT constructor
    // it is invisible and empty

    // a Class can have multiple constructors



}
